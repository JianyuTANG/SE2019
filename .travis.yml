language: python
sudo: enabled
services:
    - mysql
python:
    - "3.6"
env:
    global:
     - TRAVIS=true

# command to install dependencies
install:
    - pip install -r requirements.txt
    
script:
    python manage.py test